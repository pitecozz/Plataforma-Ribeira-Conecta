import type { NdviProduct } from "../../types/farm360";
export function NdviPanel({ product }: { product: NdviProduct | null }) { if (!product) return <section><h2>NDVI</h2><p>DADO_INSUFICIENTE — não há produto derivado persistido.</p></section>; const stats = product.statistics; return <section><h2>NDVI</h2><dl><dt>Mínimo / máximo / média</dt><dd>{stats.minimum ?? "NULL"} / {stats.maximum ?? "NULL"} / {stats.mean ?? "NULL"}</dd><dt>Pixels válidos / nodata</dt><dd>{stats.valid_count} / {stats.nodata_count}</dd><dt>Cobertura</dt><dd>{stats.coverage_percentage ?? "NULL"}%</dd><dt>Checksum</dt><dd>{product.checksum ?? "NULL"}</dd><dt>Gerado em</dt><dd>{product.generated_at}</dd><dt>Status</dt><dd>{product.processing_status}</dd></dl></section>; }
