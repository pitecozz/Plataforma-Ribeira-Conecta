import type { Scene } from "../../types/farm360";
export function SatellitePanel({ scene }: { scene: Scene | null }) { if (!scene) return <section><h2>Satélite</h2><p>UNKNOWN — nenhuma cena catalogada.</p></section>; return <section><h2>Sentinel-2</h2><dl><dt>Provider / coleção</dt><dd>{scene.provider} / {scene.collection}</dd><dt>Scene ID</dt><dd>{scene.scene_id}</dd><dt>Aquisição</dt><dd>{scene.acquisition_datetime}</dd><dt>Nuvens</dt><dd>{scene.cloud_cover ?? "UNKNOWN"}</dd><dt>Status</dt><dd>{scene.source_status}</dd></dl><h3>Assets da cena</h3><ul>{scene.assets.map(asset => <li key={asset.id}>{asset.asset_key}: {asset.title ?? "UNKNOWN"}</li>)}</ul></section>; }
