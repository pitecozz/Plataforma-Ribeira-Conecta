# Private development validation only. Copy outside Git with mode 600.
RIBEIRA_ENV=development
RIBEIRA_AUTH_MODE=development
RIBEIRA_DEV_AUTH_TOKEN=REPLACE_WITH_EPHEMERAL_LOCAL_TOKEN
# This value remains server-side; never compile it into frontend assets.
